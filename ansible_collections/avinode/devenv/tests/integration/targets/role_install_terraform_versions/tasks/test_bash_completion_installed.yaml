---
- name: Statting {{ test_file }}
  stat:
    path: '{{ test_file }}'
  register: _bash_completion_test_file_stats
- name: Asserting {{ test_file }}
  assert:
    that: |
      _bash_completion_test_file_stats.stat.exists
          and not _bash_completion_test_file_stats.stat.executable
...
